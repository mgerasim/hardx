@model IEnumerable<HardX.Models.Role>

@{
    ViewBag.Title = "Список ролей";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("Добавить роль", "Create")
</p>
<table>
    <tr>
        <th>
            Наименование
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @item.Name
        </td>
        <td>
            @Html.ActionLink("Редактировать", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Просмотр", "Details", new { id=item.Id }) |
            @Html.ActionLink("Удалить", "Delete", new { id=item.Id }, new { onclick = "return confirm('Вы действительно хотите удалить?');" })
        </td>
    </tr>
}

</table>

