@model IEnumerable<HardX.Models.Town>


@{
    ViewBag.Title = "Список населённых пунктов";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("Добавить населённый пункт", "Create")
</p>
<table>
    <tr>
        <th>
			Имя
        </th>
				
        <th>
			Район
        </th>

		<th>
			Регион
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
		<td>
            @item.Name
        </td>
		<td>
            @item.Region.Name
        </td>
		<td>
            @item.Region.State.Name
        </td>
        <td>
            @Html.ActionLink("Редактировать", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Удалить", "Delete", new { id=item.ID }, new { onclick = "return confirm('Вы действительно хотите удалить?');" })
        </td>        
    </tr>
}

</table>

